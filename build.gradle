apply plugin: 'base'

allprojects() {
    group = 'me.skidrunner'
    version = '1.0-SNAPSHOT'
}

buildscript {
	repositories {
		jcenter()
	}
	dependencies {
	}
}

subprojects {
	apply from: rootProject.file('common.gradle')
}
